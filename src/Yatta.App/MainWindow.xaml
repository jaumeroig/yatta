<ui:FluentWindow x:Class="Yatta.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
        xmlns:local="clr-namespace:Yatta.App.Markup"
        xmlns:blazor="clr-namespace:Microsoft.AspNetCore.Components.WebView.Wpf;assembly=Microsoft.AspNetCore.Components.WebView.Wpf"
        xmlns:razor="clr-namespace:Yatta.App.Blazor"
        Title="{local:Localize App_Title}"
        Height="800" Width="1100" MinHeight="600" MinWidth="800"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
        WindowCornerPreference="Round" WindowStartupLocation="CenterScreen"
        Icon="/Resources/Logo.png">
    <Grid>
        <ui:TitleBar x:Name="TitleBar" Title="{local:Localize App_Title}" ShowMaximize="True" ShowMinimize="True">
            <ui:TitleBar.Icon><ui:ImageIcon Source="pack://application:,,,/Resources/Logo.png" /></ui:TitleBar.Icon>
        </ui:TitleBar>
        <blazor:BlazorWebView x:Name="HybridView" Margin="0,50,0,0" HostPage="wwwroot\index.html" Services="{DynamicResource services}">
            <blazor:BlazorWebView.RootComponents>
                <blazor:RootComponent Selector="#app" ComponentType="{x:Type razor:AppRouter}" />
            </blazor:BlazorWebView.RootComponents>
        </blazor:BlazorWebView>
        <tray:NotifyIcon x:Name="TrayNotifyIcon" FocusOnLeftClick="False" MenuOnRightClick="True"
                         LeftClick="TrayOpen_Click" LeftDoubleClick="TrayOpen_DoubleClick"
                         TooltipText="{local:Localize App_Title}" Icon="/Resources/Logo.png">
            <tray:NotifyIcon.Menu>
                <ContextMenu Opened="TrayContextMenu_Opened">
                    <MenuItem x:Name="TrayOpenItem" Header="{local:Localize Tray_Open}" Click="TrayMenuOpen_Click" />
                    <MenuItem x:Name="TrayChangeActivityItem" Header="{local:Localize Tray_StartActivity}" Click="TrayChangeActivity_Click" />
                    <MenuItem x:Name="TrayStopActivityItem" Header="{local:Localize Tray_StopActivity}" Visibility="Collapsed" Click="TrayStopActivity_Click" />
                    <Separator />
                    <MenuItem x:Name="TrayCloseItem" Header="{local:Localize Tray_Close}" Click="TrayClose_Click" />
                </ContextMenu>
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>
    </Grid>
</ui:FluentWindow>

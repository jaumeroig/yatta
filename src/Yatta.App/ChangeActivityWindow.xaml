<ui:FluentWindow x:Class="Yatta.App.ChangeActivityWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:local="clr-namespace:Yatta.App.Markup"
    xmlns:dialogs="clr-namespace:Yatta.App.Views.Dialogs"
    SizeToContent="Height"
    Width="560"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    ShowInTaskbar="True"
    Topmost="True"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    Loaded="Window_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" ShowMaximize="False" ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="24,0,24,0">
            <TextBlock x:Name="DialogTitleText"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="0,0,0,16" />
            <dialogs:ChangeActivityDialogControl />
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="24,16,24,24">
            <ui:Button x:Name="PrimaryButton"
                       Appearance="Primary"
                       Click="OnPrimaryClick"
                       IsEnabled="False"
                       Margin="0,0,8,0" />
            <ui:Button Content="{local:Localize Button_Cancel}"
                       Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
